<div class="container">
    <h1>Empresa: <%= @risk_assistant.name %></h1>

  <div class="row">
    
    <div class="col-md-4" style="background-color:#f9f9f9">
      <h3>Instrucciones</h3>
      <h5>✅ 🔍 🗨️  🏃‍♂️♂️ 📩 ⌛📬 ⚠️  </h5>
      <p>0. Para empezar, lanzar la orden: empezar</p>
      <p>1. Para editar un campo, simplemente contesta indicando qué campo quieres cambiar y el nuevo valor.</p>
      <p></p>
      <p></p>
      <p></p>
      <p></p>
      <p></p>
      <p></p>
      <p></p>
      <% @messages.each do |mensaje| %>
        <h6><%= mensaje.key  %></h6>
        <h7><%= mensaje.value %></h7>
      <% end %>      
    </div>
  
    <div class="col-md-8" style="">
      <%= form_with url: risk_assistant_messages_path(@risk_assistant), method: :post, local: true, multipart: true do |form| %>
        <div class = "">
          <%= form.label :content, 'Escribe tu mensaje:' %>
          <%= form.text_area :content, name: 'message[content]', rows: 3, required: true, placeholder: "Escribe tu mensaje aquí..." %>
          <%= file_field_tag :file %>
        </div>
        <%= form.submit 'Enviar', class: 'btn btn-success' %>
      <% end %>

      <div id="messages">
        <% @risk_assistant.messages.order(created_at: :desc).each do |message| %>
          <div class="message <%= message.role %>">
            <strong><%= message.sender %>:</strong> <%= message.content %>
            <h3><%= @campo %></h3>
          </div>
        <% end %>
      </div>

      <% if @first_question %>
        <p><strong>Pregunta inicial:</strong> <%= @first_question %></p>
      <% end %>


      <%= link_to 'Volver a los RiskAssistants', risk_assistants_path, class: 'button' %>
    </div>

  </div>
</div>
