<!-- app/views/policy_analyses/new.html.erb -->
<div class="container py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0">Analizar nueva Póliza</h1>
    <%= link_to "Volver", policy_analyses_path, class: "btn btn-secondary" %>
  </div>

  <div class="alert alert-info mb-4">
    <strong>¿Cómo funciona?</strong><br>
    Sube una o varias pólizas en PDF. El sistema analizará automáticamente sus coberturas, exclusiones y condiciones para compararlas.
  </div>

  <div class="card shadow-sm">
    <div class="card-body">
      <%= form_with model: @policy_analysis, local: true, html: {multipart: true} do |f| %>
        <div class="mb-3">
          <%= f.label :documents, "Sube una o varias pólizas (PDF)", class: "form-label" %>
          <%= f.file_field :documents, multiple: true, required: true, class: "form-control" %>
          <div class="form-text">Puedes seleccionar varios archivos manteniendo pulsada la tecla <kbd>Ctrl</kbd> o <kbd>Cmd</kbd>.</div>
        </div>
        <%= f.submit "Analizar Pólizas", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
