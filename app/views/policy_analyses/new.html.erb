<div class="container py-4 dashboard-view">
  <div class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4">
    <div>
      <h1 class="h3 mb-1">Analizar nueva póliza</h1>
      <p class="text-muted mb-0">Sube tus documentos y obtén una comparativa automatizada.</p>
    </div>
    <%= link_to "Volver", policy_analyses_path, class: "btn btn-outline-secondary" %>
  </div>

  <div class="row g-3 mb-4">
    <div class="col-md-4">
      <div class="card shadow-sm h-100" data-state="ready">
        <div class="card-body">
          <p class="text-muted text-uppercase small mb-1">Formatos soportados</p>
          <ul class="mb-0 small ps-3">
            <li>PDF y escaneos en imagen</li>
            <li>Múltiples archivos por análisis</li>
          </ul>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card shadow-sm h-100" data-state="pending">
        <div class="card-body">
          <p class="text-muted text-uppercase small mb-1">Resultado</p>
          <p class="mb-0 small">Recibirás un resumen comparativo con coberturas, exclusiones y ranking.</p>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card shadow-sm h-100" data-state="alert">
        <div class="card-body">
          <p class="text-muted text-uppercase small mb-1">Tiempo estimado</p>
          <p class="mb-0 small">El análisis se completa en unos minutos. Te avisaremos cuando esté listo.</p>
        </div>
      </div>
    </div>
  </div>

  <div class="card shadow-sm border-0">
    <div class="card-header bg-white">
      <h2 class="h5 mb-0">Subir pólizas</h2>
    </div>
    <div class="card-body">
      <%= form_with model: @policy_analysis, local: true, html: { multipart: true, class: 'row g-3' } do |f| %>
        <div class="col-12">
          <%= f.label :documents, "Selecciona archivos", class: "form-label" %>
          <%= f.file_field :documents, multiple: true, required: true, class: "form-control" %>
          <div class="form-text">Puedes seleccionar varios archivos manteniendo pulsada la tecla <kbd>Ctrl</kbd> o <kbd>Cmd</kbd>.</div>
        </div>
        <div class="col-12">
          <%= f.submit "Analizar pólizas", class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
